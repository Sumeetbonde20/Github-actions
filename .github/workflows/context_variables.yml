name: context-variables

on: [push]
env:
  APP_NAME: MyApp
  ENVIRONMENT: production

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Printing github context variable
        run: echo '${{ toJSON(github) }}'

      - name: Printing env context variable
        run: echo '${{ toJSON(env) }}'
