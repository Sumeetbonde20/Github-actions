name: output-demo
on: [push]
jobs:
  step-output:
    runs-on: ubuntu-latest
    steps:
      - name: stage1
        id: first
        run: echo "version=10.0.1.0" >> "$GITHUB_OUTPUT"

      - name: stage1
        run: echo "Version is ${{ steps.first.outputs.version }}"
